Boot and wait for the Grub page, (hold Shift key)

Select "* Advanced options for Ubuntu" to enter "Recovery Menu"

Select "root - Drop to root shell prompt"

```
mount -rw -o remount /
passwd username
```

Ref: https://www.maketecheasier.com/reset-root-password-linux/
